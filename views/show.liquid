<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{post.title}}</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>{{post.title}}</h1>
    <p>{{post.body}}</p>
    <a href="/posts">Back to Posts</a>
    <br>
    <form action="/comment/{{post._id}}" method="POST">
        <textarea name="comment" id="body" cols="30" rows="10" placeholder="Enter Comment Here"></textarea>
    <input type="submit" />
    </form>
    <div>
        {% for comment in post.comments %}
        <h4>{{comment.author}}</h4>
        <p>{{comment.comment}}</p>
        <a href="/comment/{{comment._id}}/edit"><button>Edit Comment</button></a>
        <form action="/comment/{{post._id}}/{{comment._id}}?_method=DELETE" method="POST">
        <input type="submit" value="DELETE COMMENT" />
        </form>
    
        {% endfor %}
    </div>
</body>
</html>